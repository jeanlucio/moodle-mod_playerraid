/**
 * Cooldown timer module for PlayerRaid.
 *
 * @module     mod_playerraid/cooldown_timer
 * @copyright  2026 Jean Lúcio
 * @license    https://www.gnu.org/copyleft/gpl.html GNU GPL v3 or later
 */
define("mod_playerraid/cooldown_timer",[],(function(){return{init(){const cooldownSpans=document.querySelectorAll(".playerraid-cooldown-seconds");0!==cooldownSpans.length&&cooldownSpans.forEach((span=>{const expiry=parseInt(span.dataset.expiry,10);if(isNaN(expiry)||expiry<=0)return;const interval=setInterval((()=>{const currentTime=Math.floor(Date.now()/1e3),remaining=Math.max(0,expiry-currentTime);remaining>0?span.textContent=remaining:(clearInterval(interval),window.location.reload())}),1e3)}))}}}));

//# sourceMappingURL=cooldown_timer.min.js.map